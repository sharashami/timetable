$(function() {
    // var url = $(location).attr('pathname');

    // url = url.split("/", 3).join("/");

    // $('#nav li a[href |= "' + url + '"]')
    //     .parent()
    //     .addClass('active');
});